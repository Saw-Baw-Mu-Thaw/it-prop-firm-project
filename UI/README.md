# Instructions 
Run db.sql in postgres
install requirements.txt
from inside UI folder, run `flet run src/main.py`
from inside Backend folder, run `fastapi run app.py`

# Known Issues
UI does not load strategies from db
UI haven't connected backtest frontend with backend
/connect endpoint needs to be run once to store broker login and password